import * as React from "react"
import { Text, Struct } from "components"

const UnderConstruction: React.FC = () => (
  <Struct>
    <Text.Body>
      This page is being updated, come back later.
    </Text.Body>
  </Struct>
)

export default UnderConstruction
